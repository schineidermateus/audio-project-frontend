import { AUDIO_PATHS } from "./path.constants";

export const AUDIO_ROUTES = {
    base: `/${AUDIO_PATHS.base}`,
    join: `/${AUDIO_PATHS.base}/${AUDIO_PATHS.join}`,
    cut: `/${AUDIO_PATHS.base}/${AUDIO_PATHS.cut}`,
    mix: `/${AUDIO_PATHS.base}/${AUDIO_PATHS.mix}`,
}